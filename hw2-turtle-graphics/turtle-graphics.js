class Turtle{
    constructor(x,y){
        this.x = x;
        this.y = y;
    }
}

new Turtle (0,0)

Turtle.prototype.forward = function(forward) {
    return new Vector(
       this.forward = forward;
    
   }